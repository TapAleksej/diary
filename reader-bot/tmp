bot:
    build:
      context: ./bot
    container_name: bot-container
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    restart: always


RUN apt-get update && \
    apt-get install -y curl && \
    rm -rf /var/lib/apt/lists/*